pipeline {
    agent any

    stages {
        stage('node.js Deps') {
            steps {
                sh'npm install'
            }
        }

        stage('E2E Tests') {
            steps {
                sh 'npx playwrigth test'
            }
        }


       
    }
}
